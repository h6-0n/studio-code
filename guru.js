function vote() {
    var name = document.getElementById("name").value;
    var pass =100*Math.random();
    var result = document.getElementById("result").value;
    result.innerHTML = name + ", Passes with"

}